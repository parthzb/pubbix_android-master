package com.pubbix.feature.common;

public interface Enums {
    enum ListingType {
        PUBLISHED, UNPUBLISHED, SOLD
    }

    enum EditTextType {
        NAME, EMAIL, PHONE, GENDER, BIOGRAPHY, IDENTITY
    }

    enum SnackBarMessageType {
        INFO, WARNING, ERROR
    }
}
