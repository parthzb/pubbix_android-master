package com.pubbix.util.brick;

public final class BrickHelper {
    private BrickHelper() {
    }

    public static final int MAX_SPANS = 240;
    public static final int FULL_WIDTH = MAX_SPANS;
    public static final int HALF_WIDTH = FULL_WIDTH / 2;
    public static final int THIRD_WIDTH = FULL_WIDTH / 3;
    public static final int QUARTER_WIDTH = FULL_WIDTH / 4;
}
